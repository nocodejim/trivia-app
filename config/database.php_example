<?php
/**
 * Database Configuration - EXAMPLE
 *
 * This is a template for the database configuration.
 *
 * --- INSTRUCTIONS ---
 * 1. DO NOT EDIT THIS FILE DIRECTLY.
 * 2. COPY this file and RENAME the copy to `database.php`.
 * 3. The new `database.php` file is already in .gitignore and will NOT be committed.
 * 4. Fill in your actual database credentials in the new `database.php` file.
 */

// --- PDO CONNECTION SETUP ---
$charset = 'utf8mb4';

// Data Source Name (DSN)
$dsn = "mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=" . $charset;

// PDO connection options
$options = [
    PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
    PDO::ATTR_EMULATE_PREPARES   => false,
];

// Global PDO object
$pdo = null;

try {
    $pdo = new PDO($dsn, DB_USER, DB_PASS, $options);
} catch (PDOException $e) {
    error_log("Database Connection Failed: " . $e->getMessage());
    http_response_code(503);
    header('Content-Type: application/json');
    echo json_encode([
        'success' => false,
        'error_code' => 'DATABASE_CONNECTION_ERROR',
        'message' => 'Service is temporarily unavailable. Please try again later.'
    ]);
    exit;
}

/**
 * --- ENVIRONMENT-SPECIFIC CREDENTIALS ---
 *
 * Uncomment the block that matches your environment inside your
 * new 'database.php' file.
 */

/*
// --- FOR DOCKER (Local Development) ---
define('DB_HOST', 'db');
define('DB_NAME', 'trivia_db');
define('DB_USER', 'user');
define('DB_PASS', 'password');
*/

/*
// --- FOR XAMPP (Local Development) ---
define('DB_HOST', 'localhost');
define('DB_NAME', 'trivia_db');
define('DB_USER', 'root');
define('DB_PASS', '');
*/

/*
// --- FOR HOSTINGER (Live Production) ---
define('DB_HOST', 'localhost');
define('DB_NAME', 'YOUR_HOSTINGER_DATABASE_NAME');
define('DB_USER', 'YOUR_HOSTINGER_USERNAME');
define('DB_PASS', 'YOUR_STRONG_PASSWORD');
*/

